[project]
name = "timesheet-annoying-automation"
version = "0.1.0"
description = "Playwright automation for submitting Irish EOR timesheets on Deel"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40",
]
 
[project.scripts]
timesheet = "timesheet:main"
